import React from 'react';
import ReactDOM from 'react-dom/client';
import BounceBlob from '../bounceblob.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <BounceBlob />
  </React.StrictMode>
);
